# hellotest-consumer

Consumer of hellotest — used to test sentinel cross-repo search.

This repo contains code that depends on the `compute_total(price, quantity, tax_rate, discount)` function signature from `krmayankk/hellotest`.
